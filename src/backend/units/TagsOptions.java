package backend.units;

/**
 * describes possible allowed tags for any unit
 */
public enum TagsOptions {
    LARGE,
    SMALL,
    // statuses
    DEAD,
    // attacking types
    MELEE,
    RANGED
}